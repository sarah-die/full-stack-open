// an arrow function is assigned to App. Note: here in shortform because the function only consists of a single expression
const App = () => (
  <div>
    <p>Hello world</p>
  </div>
);

export default App;
